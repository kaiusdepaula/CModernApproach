/*
Suppose that str is an array of characters. Which one of the following statements is not
equivalent to the other three?
(a) *str = 0; -> is not equivalent.
(b) str [0] = '\0';
(c) strcpy (str, "" ) ;
(d) strcat(str, "");
*/

#include <stdio.h>

int main(void){

    return 0;
}